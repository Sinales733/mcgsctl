# Third Party Notices

`mcgsctl` uses the local .NET runtime and a Java/Jackcess helper for read-only `.MCE` export.

The `lib/` directory contains Java dependency JARs required by Jackcess. Keep their original license files and SHA256 hashes with any release package.
